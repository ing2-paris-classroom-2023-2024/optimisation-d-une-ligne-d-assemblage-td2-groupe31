#include <stdio.h>
#include "graphe.h"
#include <stdlib.h>
#include "header.h"
#include <unistd.h>

int main () {
    int a;
    menu();
    printf("bonjour : ");
    scanf ("%d",&a);
    /*Graphe *graphe;
    graphe = initGraphe();
    precedence(graphe);
    cycle(graphe);
    return 0;*/
}