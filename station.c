#include "header.h"
#include "stations.h"
#include <stdlib.h>

void station () {
    //Faire les stations
    /*Usine *usine= (Usine *) malloc(sizeof(Usine));
    usine->pStation = (pStation *) malloc(tab[0] * sizeof(pStation));
    for(int i = 0; i < tab[0]; i ++) {
        usine->pStation[i] = (pStation) malloc(sizeof(struct Station));
        usine->pStation[i]->num = i;
        usine->pStation[i]->operation = NULL;
        for(int j = 0; j < graphe->ordre; j ++) {
            if (graphe->pSommet[j]->precedence == i) {

            }
        }
    }*/
}